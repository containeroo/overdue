apiVersion: v1
kind: Service
metadata:
  name: overdue-prometheus-watchdog
spec:
  selector:
    app.kubernetes.io/instance: prometheus-watchdog
    app.kubernetes.io/name: overdue
  ports:
    - name: http
      port: 8080
      targetPort: http
